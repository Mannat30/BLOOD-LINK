spring:
  application:
    name: bloodlink-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/bloodlink_db
    username: postgres
    password: mannat
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      fail-on-empty-beans: false

# =========================
# JWT CONFIGURATION
# =========================

jwt:
  secret: BloodLinkAI2026SuperSecretKeyForJWTAuthentication123